# Project Status — Bhookmark

Last updated: 2026-09-14

> Note on naming: the app was built under the name "Project Palate" / "Palate" and has been renamed to **Bhookmark** (Bhook + Bookmark). **The rename is complete** in the codebase: every user-facing string, the page title, `package.json`, the journal screen/tab (renamed `Palate.tsx` → `Bhookmarks.tsx`, tab id `palate` → `bhookmarks`), localStorage keys, service worker copy, and backend log/comment references all say Bhookmark now. The GitHub repo and Vercel project slug are being renamed next (approved by the user — GitHub auto-redirects the old URL, Vercel just changes the default `*.vercel.app` subdomain). Copy was also reworked to lean into the "bhookmark" verb: the Home headline ("What are you bhookmarking now? 😏🤤😂"), the "would reorder" stat ("Would bhookmark again"), and the verified-badge/empty-state copy that used to say "worth ordering again" / "whether to reorder."

## What has been completed

### Phase 1 — Trust and data correctness (from the implementation brief) — closed
- Evidence-aware taste intelligence: 3-tier gating (early hint at 3 logs, signature craving at 5 logs/3 venues, confidence bands) — `src/evidenceThresholds.ts`, wired into `Profile.tsx` and `Bhookmarks.tsx`.
- Score separation: Your / Community / Verified-only ratings, each with real sample sizes and a confidence band, no blended number — `server/src/routes/dishes.ts` (`GET /dishes/score`), `server/src/evidence.ts`.
- Real geolocation verification: BiteLog calls `navigator.geolocation` for real; server cross-checks against known venue coordinates (haversine) — `server/src/routes/logs.ts` (`computeLocationMatch`).
- Ranking-manipulation controls, all real, all tested, all **hold for review** (never auto-block):
  - Venue burst (same device+venue, >5 in 10min)
  - Multi-account/same-network (4+ distinct accounts from one IP in 24h) — needed `app.set("trust proxy", true)` in `app.ts`, which was also a real pre-existing gap (IP-based rate limiting had been silently broken since the Vercel deploy)
  - Repeated delete-and-repost (3rd cycle at the same venue in a day)
  - Impossible travel (two location-verified logs too far apart, too fast — calibrated to this app's real ~9km max venue spread)
- Restaurant self-rating disclosure: venue-claim → moderator-approval flow (`venue_claims` table); an approved owner's own logs at their venue are excluded from every public aggregate but still show (labeled) in their own journal — `server/src/routes/venues.ts`, `server/src/routes/moderation.ts`, `src/screens/Profile.tsx` ("Run a restaurant?" card).
- Credibility layer: `WhyThis` component wired into dish-score breakdowns, recommended-pick reasons, and the verified badge in BiteLog.
- Real trending signal: `GET /dishes/trending`, a genuine 7-day log count with a minimum-evidence floor (5 logs / 3 distinct users) and QA-fixture exclusion — replaces an earlier "Trending" label that had no real time-window behind it (a real bug this session caught in its own prior work and fixed).
- **Explicitly closed, not deferred**: creator-led brigading and paid/sponsored-content disclosure. Both need infrastructure (a real social graph, a business model for sponsorship) that doesn't exist and isn't being built. Not on the list anymore, by the user's decision.

### AI dish recognition — built, then fully removed by decision
A complete real-vision backend (Claude vision via tool-use, honest failure modes, daily+global cost caps) was built, tested, and verified. The user then decided — after seeing the real, verified per-call cost (~$0.005–0.01, confirmed against Anthropic's actual published pricing) — that **any** ongoing paid cost was unacceptable, even a fully bounded ~$12/month worst case. All of it was removed, not left dormant:
- Deleted: `server/src/vision.ts`, `server/scripts/vision-test.mjs`
- Removed: `/logs/detect`, `/logs/detect/status`, `/logs/correction` routes and their cost-cap logic; `/dev/ai-corrections`; `db.ts`'s `recordAiCorrection`/`listAiCorrections`; the `ai_corrections` table from `schema.sql` (the table itself still exists in the live prod DB with a few test rows — not dropped without an explicit ask)
- `LogFlow.tsx` capture step reverted to a plain optional photo attach (Taste Receipt only, never analyzed)
- **This is a closed decision, not a paused one.** Don't re-propose AI vision unless the user brings it up.

### Privacy visibility (Phase 2 item, just completed)
Per-log `visibility: "private" | "public"` (public is the default — deliberate, since this app's core loop is a shared community score, not a private-first diary). A private log:
- Still posts, still goes through all the same evidence/ranking checks
- Still shows in the owner's own Bhookmarks journal (labeled 🔒 Private)
- Is excluded from all four public read paths: `listPublishedLogs`, `listPublishedLogsByCategorySubtype`, `listPublishedLogsForDish`, `getTrendingDish`

UI: a toggle in BiteLog's confirm step (`src/screens/LogFlow.tsx`). "Circle-only" visibility is explicitly not built — Circles has no real backend to be visible *to* yet.

### Visual redesign (Phase 5, mostly done)
Home/Crave: circular real-photo category rail, a "Trending"/"Top rated" spotlight card, photo-forward "Recommended for you" cards, category-accent colors, page-transition motion.

Extended to the rest of the app this session: `CATEGORY_ACCENT` and `findDishPhoto` moved to `src/data/dishes.ts` (shared, no longer duplicated in Home.tsx) so every screen reads from one source.
- **Bhookmarks** (`src/screens/Bhookmarks.tsx`): log cards now use a larger photo-forward `DishThumb` (real catalog photo when the log matches one, via `findDishPhoto`) with a category-accent pill badge overlaid on the corner.
- **Profile**: every card now fades in staggered on load (previously zero motion); the Signature Craving card shows a category-accent pill once a pattern is unlocked or hinted.
- **Circles**: `FoodCircles.tsx` gained a conic-gradient match-score ring and avatar-initial stacks per circle; `RemixableLists.tsx` gained a colored accent spine + tinted clone-count badge per list; both use the same stagger-fade motion as everywhere else. `CravingRoom.tsx` already had this treatment and was left as-is.

### Accessibility audit (Phase 5, closes it out)
- **Contrast**: `text-faint` (used 96x app-wide) measured 3.58:1 against the main background — below WCAG AA's 4.5:1 for normal text. Fixed at the token level (`tailwind.config.js`: `#5B6D69` → `#7B8D89`), which fixed every usage at once since it's never used for anything but text color.
- **Tap targets**: 5 real interactive controls were 32-36px (back buttons in Home/NearMe, LogFlow's close button, Home's carousel prev/next) — bumped to 44×44 (`w-11 h-11`), the WCAG minimum. Checked every other small element found (avatars, match-rings, decorative chips) and confirmed they're non-interactive, so left alone.
- **Screen reader labels**: added `aria-label` to icon-only controls that had none (CravingRoom's ✕/♥ swipe buttons, Home/Login/Profile's placeholder-only text inputs, the two radius `<input type="range">` sliders). LogFlow's inputs were already properly wrapped in `<label>` — no change needed there. Fixed a heading-order slip in `NearMe.tsx` (`<h2>` → `<h1>`, since it's the sole heading when that screen is the active view).
- **Reduced motion**: found the existing `prefers-reduced-motion` CSS rule in `index.css` only catches native CSS transitions — every `motion.*` component (all the page fades, stagger-ins, swipe cards) is framer-motion, driven in JS, and doesn't respect that rule at all. Fixed by wrapping the app root in `<MotionConfig reducedMotion="user">` (`src/main.tsx`) — framer's own native equivalent, so vestibular-safe users get it everywhere at once.
- **Images**: audited every `<img alt="">` (dish/venue photos) — all are adjacent to visible text naming the same dish/venue, so decorative `alt=""` is correct, not an oversight.

Verified: both typechecks clean, all 89 backend + 4 IP-isolation + 15 frontend tests still pass, spot-checked live in a browser (44px hit areas confirmed via `getBoundingClientRect()`, new contrast visibly more legible).

### Also fixed this session
- Horizontal swipeable results deck (Tinder-style) replacing a bland vertical list capped at ~2 static demo dishes; expanded the Pizza catalog to 10 real Bangalore chains.
- Added Momos as a full category (real chain: Wow! Momo).
- Real geography + weather personalization (`src/smartPicks.ts`) — reverse-geocodes via OpenStreetMap Nominatim, pulls live weather via Open-Meteo, reorders categories. Explicit opt-in, never silent. **Caveat**: Nominatim's usage policy doesn't permit heavy production client-side traffic — fine at current scale, needs a paid geocoding provider + server-side caching before real scale.
- Festival-based suggestions were considered and explicitly **not** built — no verified 2026 festival dates exist to hardcode, and guessing would be exactly the kind of fabricated-confidence bug this whole session has been removing elsewhere.

### "Food-first tactile futurism" redesign (post-Phase-5, from a detailed user design brief)
The user supplied a full redesign brief (new palette, per-screen redesigns, and a request for real WebGL/R3F, Rive, and Spline for three "hero" depth moments). Followed the direction but substituted the 3D-library asks with CSS + the Framer Motion already in the stack — same felt effect, none of the bundle-size/GPU/asset-production cost for a zero-budget solo project. Explicitly told to use judgment rather than follow it literally.
- **Palette**: `tailwind.config.js` now uses the user's exact hex values (bg `#070908`, surface `#101513`, elevated `#171E1B`, turquoise accent `#31E2D1`, warm text `#F5F1E8`). Turquoise is action-only now; added `saffron` (`#FFB547`, discovery/trending — renamed from the old flat `gold` token) and `coral` (`#FF6659`, social reactions) as distinct roles, plus per-color `shadow-*Glow` utilities for "food-coloured" hover shadows. Reassigned real usages to match: Bhookmarks' reaction chips (turquoise → coral), "Flavor DNA"/digest labels (turquoise → saffron, since those are discovery cues, not actions).
- **Home/Crave** (`CravingOrb.tsx`, `TrendingStack.tsx`): a pointer-reactive glowing orb behind the heading (CSS + `useMotionValue`/`useSpring`, tints toward the hovered category's color, respects `useReducedMotion`) stands in for the requested R3F orb. The single flat spotlight rectangle became a real draggable 3-card fanned stack (trending + up to 2 real recommended picks, never padding with filler). Category rail and Recommended cards get a 2° tilt + food-colored shadow on hover. The static 3-emoji heading is now one contextual emoji — real weather mood when resolved, else real local time-of-day, never a fabricated mood. The location banner becomes a compact `📍 {area}` pill using a real neighborhood-level value now captured from Nominatim (`smartPicks.ts` gained `area`/`weatherMood` fields) — deliberately **not** a distance figure, since nothing on this screen is anchored to one venue to measure distance against.
- **Circles** (`CravingRoom.tsx`): setup screen restructured into the requested 3-step guided flow — "Who's hungry?" (avatars now animate in staggered), "What's the vibe?" (mood pills → photo-backed tiles using real catalog images), "How far?" (a custom SVG radius ring with +/− steppers, replacing the native slider). The CTA shows a live match count that's a real filtered count of the actual catalog by mood category, never a placeholder number. The reveal step's unanimous pick now visually "emerges" (scale/glow entrance + a fading radial pulse) with "Table's set." framing instead of a flat list item.
- **Profile → Passport** (`PassportCard.tsx`, `BottomSheet.tsx`): the flat avatar-row-plus-stats became a collectible card — CSS 3D tilt that tracks the pointer (`rotateX`/`rotateY` off pointer position, a holographic radial sheen that follows it, a rotated "VERIFIED MEMBER" stamp), a real derived level badge (New Tongue / Regular / Local Legend / City Icon, from actual dishes-logged count). The phone number is masked by default (`+91 •••• ••45`, tap to reveal) — was previously shown in full. The empty "0 0% 0" stat block for new users now sits next to a real progression banner ("Log 3 dishes to unlock your Flavor DNA — 1/3", using the app's actual existing `EARLY_LOGS_NEEDED` threshold, not an invented number). Dietary profile/allergens, the restaurant-claim form, and devices/sessions all moved off the main scroll into `BottomSheet` modals — same functionality, progressive disclosure instead of an account-settings wall.
- **Not built, by design**: R3F/three.js, Rive, and Spline were not added as dependencies — the CSS/Framer-Motion equivalents above cover the same three "hero" moments (orb, tilt card, match reveal) the brief called for. DPR-capping/offscreen-pausing/static-fallback concerns from the brief don't apply since there's no WebGL canvas to manage.
- **Known test-environment limitation**: full-page visual QA hit a sandbox-specific quirk where `#root`'s `height:100%` + `overflow:hidden` (pre-existing, unrelated to this work) doesn't grow past one viewport in this session's browser-automation tool specifically (confirmed via injecting a raw untouched test div, which hit the identical cap) — verified via DOM/text-content checks and partial screenshots instead. Real mobile browsers don't share this rendering surface, so this shouldn't affect actual users, but a real on-device pass would be worth doing before shipping this further.
- **Two real bugs the user caught live on their own device**, fixed same-day: fonts were loading only via a CSS `@import` (discovered late, can lose the race on a real network — the page was silently stuck on the browser's fallback bold font, reading as a dated look) — moved to `<link>` tags in `index.html`. Colored card shadows were silently broken — two separate Tailwind `shadow-*` classes were stacked on one element, but both set `box-shadow`, so only one could ever win; combined lift+color into single values per glow token. Also fixed `TrendingStack`'s hardcoded 220px container height overlapping the section below it on a real (narrower) phone — replaced with a responsive aspect-ratio reservation.

### Second palette/motion pass — "liquid glass" (same day, user pushed further after seeing v1 live)
User watched a reference video of trending app UIs and pushed for a more tactile, "floating liquid glass" feel than the first pass delivered, plus said the turquoise action color and Archivo headline font both read as flat/boring. Tried Google Stitch (an AI design generator) per the user's ask first — it didn't render through the browser-automation session (stuck behind Google's sign-in flow) — so implemented directly instead, per the user's own follow-up decision.
- **Palette pivot**: `accent` is now a purple/pink/magenta family (`#E879F9` flat representative; `accentInk` flipped from near-black to white since a magenta background needs light text, not dark) — saffron/coral unchanged. Real `.gradient-primary` (violet→fuchsia→magenta) applied to the highest-visibility actions (bottom-nav FAB); `accent` alone (still the new magenta, just flat) still covers the many other buttons across 13 files that weren't worth a full sweep. Found and fixed several hardcoded old-turquoise hex literals the token rename didn't catch (`CravingOrb.tsx`'s per-category colors, the radius-ring stroke, the match-score ring, the `.input` focus style) — all now the new magenta.
- **Font**: `font-display` swapped from Archivo to **Unbounded** (bold, geometric, distinctive) — Hanken Grotesk stays for body text.
- **Gradient text**: new `.text-gradient` utility (violet→fuchsia→pink, clipped to text) applied to the app's primary H1 headlines (Home, Bhookmarks, Login, the Passport card's name) — answers "don't keep headings plain white."
- **Glassmorphism ("Liquid Glass")**: `.bg-surface` now gets `backdrop-filter: blur(24px) saturate(160%)` plus a translucent background, applied globally via one CSS rule after `@tailwind utilities` (same specificity, wins the cascade) rather than touching every card's component file — retroactively glass-ifies every card app-wide. Deliberately scoped to `bg-surface` only, not `bg-surface2` (small chips/pills use that) — a `backdrop-filter` per element is real GPU cost, and this app can have dozens of small `surface2` chips on one screen versus far fewer real cards.
- **Floating**: every `.rounded-card` now gets a real lift shadow by default (previously opt-in via `shadow-lift` per component) — same global-CSS-override technique.
- **Haptics** (`src/haptics.ts`): a small `navigator.vibrate()` wrapper, wired into the bottom-nav tabs/FAB, star rating, Duel/CravingRoom swipe decisions, and the "Table's set" match reveal. **Honest limitation, stated to the user**: iOS Safari has never implemented the Vibration API (an Apple platform restriction, not fixable from a web app) — this only actually buzzes on Android Chrome/Firefox; everywhere else it's a silent no-op.
- **Scroll-triggered reveals**: Bhookmarks' journal cards, `RemixableLists`, and `FoodCircles` switched from mount-based `animate` to `whileInView` (fires as each card scrolls into the viewport, not just once on page load) — matters once these lists grow longer than one screen.
- **Not done**: a full sweep of every remaining flat `bg-accent` button to the literal 3-stop gradient (13 files use it) — the color token itself already carries the new magenta everywhere, so the palette shift landed globally regardless; only the handful of most-visible CTAs got the full gradient treatment for now.

### Phase 1 stabilization + P0 security fixes (2026-09-10)
A prior session (2026-09-08) had already written most of the fixes below directly into the working tree, but **never committed or deployed them** — this session found that, finished the incomplete parts, verified everything for real, and shipped it. Between the two sessions this closes out "Phase A" of the implementation brief referenced throughout this doc.
- **Scroll/gesture bugs (V01–V06)**: touch-action fixed on all swipeable cards (was blocking native vertical scroll entirely — the most likely cause of the user's "scrolling gets stuck" report), BottomSheet's drag-handle and scrollable content properly separated, modals portal to a stable root, TrendingStack keyed by stable dish id instead of array index. Only V03 (`.bg-surface`'s 24px backdrop-blur) was still unaddressed; cut to 14px with `contain: paint` today.
- **The "Filter Coffee" → "Coffee" category rename**, started 2026-09-08 but incomplete, was actually broken in two ways: it left the production build (`tsc -b`) failing outright, and (separately) left `server/src/venues.ts` still tagged `"Filter Coffee"` while the frontend sent `"Coffee"` — an exact-string mismatch that made Near Me's Coffee search silently return zero results, always. Both fixed. **This does not mean "all cafes now show up"** — the app still only has 13 hardcoded venues total; real coverage at city scale is Phase 2 work (see Future tasks).
- **Fake reviews, fixed at the display layer**: `Home.tsx`'s recommended carousel, browse card, trending stack, and "Consensus notes" were all rendering static seed `score`/`tasteNotes` fields through the same UI as real evidence-gated community data, with no distinction. New `EvidenceScoreBadge` component only shows a number once `community.count > 0`; `/dishes/score` now returns real log notes instead of hand-authored copy. The underlying recommendation *ranking* (`server/src/recommend.ts`) still reads the static catalog score internally — that's a Phase 2 fix, flagged as the highest-risk step there.
- **A real, separate "fake reviews" cause found live today, not in the brief**: `getTrendingDish()`'s QA-fixture exclusion only ever checked the `venue` column; a real test fixture ("QA Travel Dosa", logged against the real venue "CTR (Shri Sagar)" by `qa-brief-tests.mjs`'s impossible-travel check) was surfacing as the actual "trending" pick in production, discovered while manually verifying today's deploy. Fixed by also excluding on `name`. This is a live instance of "Current problems" item 1 below — worth an actual audit pass, not just this one instance.
- **P0 security fixes shipped**: real OTP RNG + Google Sign-In (`GOOGLE_CLIENT_ID`/`VITE_GOOGLE_CLIENT_ID` now configured, consent screen published to Production — any Google account can sign in) replacing the `devOtp` shortcut, which is now locked out in production entirely (no SMS vendor exists, so **Google Sign-In is the only login path in production right now**); consent-gated friend requests instead of instant mutual-add with phone exposure; room-swipe membership + expiry enforcement; server-verified location evidence instead of client-asserted photo/receipt booleans; identity-based rate-limit bucketing instead of a spoofable raw header; a fail-closed test-database guard (`server/scripts/assertTestDatabase.mjs`).
- **A real deploy-time outage, caused and fixed same session**: adding `google-auth-library` only to `server/package.json` (not root `package.json`) took the entire API down in production (`FUNCTION_INVOCATION_FAILED`, "Cannot find module") for a few minutes — Vercel's build only ever runs `npm install` at the repo root, so every server runtime dependency has to be duplicated into root `package.json` too (an existing, if unusual, pattern this project already relied on for express/zod/jsonwebtoken/etc. — just missed for this one). Rolled back immediately, root-caused, fixed, redeployed, fully verified. Also removed `@anthropic-ai/sdk` from root `package.json` (confirmed unused, leftover from the already-removed AI vision work).
- A real, pre-existing bug unrelated to the brief was also fixed in passing: `PassportCard.tsx` called a Framer Motion hook (`useTransform`) conditionally inside JSX, a rules-of-hooks violation that could crash the You page if reduced-motion toggled at runtime.
- **Verification**: full build (frontend `tsc -b` + `vite build`, server `tsc --noEmit`) and lint clean; 38/38 `phase4-test.mjs` and 13/13 `redteam-smoke.mjs` pass against the real database; every fix manually confirmed against live production data via curl and a real browser session (Coffee search returns real venues with honest "starting score" labeling, trending returns `null` instead of fake data, no console errors).

### Phase 4 — Craving Rooms / Food Circles / Lists (2026-09-05, real backend built and shipped)
Previously fully mocked. Now a real backend, zero paid APIs (polling instead of WebSockets, code/link invites instead of SMS):
- **Friends**: every user gets a permanent, unique friend code (`GET /friends/code`); adding someone by code (`POST /friends/add`) creates a mutual friendship in one call. Circles are built from this real friends list, not invented contacts.
- **Food Circles**: `POST /circles` validates every member against the creator's real friends list (silently drops non-friends); `computeCircleMatchScore` is a genuine Jaccard-style intersection/union of members' real loved-categories from the `logs` table — an honest 0% when there's no real overlap yet, never a placeholder number.
- **Craving Rooms**: disposable code-based sessions. The creator's client computes real candidate dish IDs from the mood/category the group picked; that exact candidate list is stored server-side (`candidate_ids`) so every participant swipes on the identical set. Reveal (`GET /rooms/:id/reveal`, polled every 2.5s) computes real unanimous (liked by everyone) and partial (liked by some) matches from actual recorded swipes — no simulated "waiting for others."
- **Lists**: real creation, a real cross-user feed, real clone lineage (`parent_list_id`) and real clone counts.
- New tables: `friendships`, `circles`, `circle_members`, `craving_rooms`, `craving_room_participants`, `craving_room_swipes`, `lists`, `list_items`, plus a `friend_code` column on `users` — all migrated live against the production Neon DB. 30 new backend tests (`server/scripts/phase4-test.mjs`), all passing; zero regressions across the existing 89 backend + 4 IP-isolation tests.
- **A real bug found in live QA after shipping, same day**: `AnimatePresence mode="wait"` (and, it turned out, even its default non-"wait" mode) would intermittently hang on exit-completion in this app — the visible screen would freeze on the previous tab/card while React's own state had already moved on, with no error thrown. First caught as the Circles tab not rendering after a click; a second instance surfaced independently in the Craving Room's swipe card after the first fix was already live. Fixed by removing `AnimatePresence` from every key-based screen/card transition (top-level tabs, Circles' sub-tabs, Home's result carousel, Duel's swipe cards, Craving Room's swipe card, TrendingStack's card cycler) in favor of plain mount-in-only animation — transitions lost the exit-fade flourish but are now both correct and faster. Deployed as a same-day follow-up fix after the first Phase 4 deploy.
- **QA note**: the earlier automated `phase4-test.mjs` runs (and this session's live click-through QA) write into the same production Neon database the live app reads from — there's no separate dev/staging DB. All synthetic test accounts and their lists/circles/rooms created this session were identified and deleted afterward (cascades cleanly via `ON DELETE CASCADE`). Worth remembering next time an automated test suite runs: it's writing to prod, and its rows are visible in shared surfaces like the Lists feed until cleaned up.

## Important decisions (so future work doesn't re-litigate these)
1. **No paid APIs, period**, until there's a revenue model. This closed AI vision recognition entirely (see above) — don't propose it again unassisted.
2. **No photo persistence.** No object storage exists; nothing is stored beyond the duration of a request. This also closes off photo-reuse/receipt-reuse detection (Phase 1 item 5) until this changes.
3. **Public-by-default logging**, not private-by-default — a deliberate departure from the brief's generic guidance, made because this app's product is a shared community score.
4. **Minimum-age / parental-consent policy: not needed** — the user's explicit call, not to be second-guessed or re-raised.
5. **Domain**: recommended `.com` via Porkbun or Cloudflare Registrar (flat $11.08/year, no renewal bait-and-switch) over `.in` (cheaper but reads as India-only) or `.app` (real 70% renewal price jump). User has not yet purchased a domain.
6. **Rename to "Bhookmark"** — done in-app; GitHub repo + Vercel slug rename in progress (user approved renaming both).
7. **1M-user-scale performance/load testing**: permanently out of scope, per explicit user instruction.
8. Reliability tests (network-drop mid-upload, background-job idempotency) beyond what's already covered: deferred, not urgent.

## Current problems (known, unresolved)
1. ~~Test data pollution in the production DB~~ — **cleaned up** (2026-09-05), for the Phase 4 tables specifically: deleted all synthetic accounts from `phase4-test.mjs` runs and this session's live QA (cascaded their lists/circles/rooms). The general lesson stands — this project has no separate dev/staging DB, so any automated test run or manual API-based QA writes directly into the same database the live app reads from, and anything landing in a cross-user surface (the Lists feed, mainly) is visible to real users until cleaned up. Other, older test fixtures from earlier phases (pre-dating this cleanup) haven't been specifically re-audited. **Confirmed still real (2026-09-10)**: a `qa-brief-tests.mjs` fixture ("QA Travel Dosa" logged at the real venue "CTR (Shri Sagar)") was live in the `logs` table and surfaced as the actual "trending" pick in production until caught during today's deploy verification — the trending query's exclusion filter is now patched for this one case, but the underlying rows are still sitting in prod (not deleted — that's a deliberate, deletion-is-destructive call, not an oversight) and no one has swept the rest of the table for similarly-named fixtures from other test scripts.
2. **The orphaned `ai_corrections` table** still exists in production Postgres (schema.sql no longer creates it on fresh deploys, but the existing table wasn't dropped). Harmless, just dead weight.
3. ~~The `llm.ts` optional AI recommendation-blurb layer~~ — **removed** (2026-09-04). It was a second, separate paid-API integration point from the already-removed vision code — inert (no `ANTHROPIC_API_KEY` set) but still live in the code. Deleted `server/src/llm.ts`, its wiring in `routes/recommendations.ts` (now just returns the deterministic template `reason` directly, no `reasonSource`/`llmConfigured` fields), the `@anthropic-ai/sdk` dependency, and the matching frontend/test references. All 93 tests still pass.
4. ~~No custom domain yet~~ — **resolved**. `bhookmark.com` is purchased, DNS-wired, and live in production (verified serving the latest deploy as of 2026-09-05).
5. ~~Stray Vercel project~~ — **resolved (confirmed 2026-09-14)**: `vercel project ls` no longer lists `project-palate`, only `bhookmark`. The lesson below still stands. Original note, **stray Vercel project created by mistake (2026-09-13)**: `.vercel/project.json` had gone stale (pointed at a project ID Vercel no longer recognized under this account), so a routine `vercel link --yes` — meant to just re-confirm the existing link — instead silently created a brand-new, separate project named `project-palate` (id `prj_TC4j0eewcGBwr0pnfbLhododd7hI`) and connected it to the same `NJ-0803/bhookmark` GitHub repo, rather than relinking to the real `bhookmark` project (id `prj_yIlCY2b5jYCr7jZxdREEPMafwXYc`). Caught immediately (before any deploy went to it) and `.vercel/project.json` was hand-corrected back to the real project before deploying — production was not affected. The stray `project-palate` project is still sitting in the Vercel account, empty (no deployments), with a GitHub connection that could double-trigger on a future push if left as-is. Not deleted automatically (deleting a project is a destructive, user-facing call) — needs the user to either delete it in the Vercel dashboard or explicitly ask for it to be removed. Lesson: never pass `--yes` to `vercel link` when `.vercel/project.json` already exists and looks stale — run it interactively (or `vercel project inspect <known-name>` first) so a name mismatch surfaces before it creates something new.

## Future tasks (not started, in rough priority order per the user's own stated interest)
- **Catalog rebuild (approved plan, "Phase 2" — heaviest, not yet started)**: replace the three drifting hardcoded catalogs (`server/src/catalog.ts`, `server/src/venues.ts`, `src/data/dishes.ts`) with a real Postgres-backed `venues`/`dishes` schema, sourced from live OpenStreetMap Overpass API data (free, no key) rather than hand-maintained arrays — the only realistic way to cover a real city's worth of cafes/restaurants. Also: a broader category taxonomy (the current 7 categories don't cover things like Pav Bhaji/chaat/North Indian/Chinese at all), an alias table so a category-string rename can never silently break search again, a lightweight venue-submission path, and moving `recommend.ts`'s ranking off the static catalog score (flagged as the single highest-risk step — do it last, with full attention). Estimated 14-18 focused hours, meant to span several sessions. Full design already written up; ask to see the plan rather than re-deriving it.
- **Phase 3 — in progress (2026-09-13, uncommitted)**: user redefined the look as black-dominant with a "blood gradient" red accent (~10-15%), reference: Audi RS e-tron GT interior. Done: accent tokens, gradients, ambient glows, craving orb, star/ring/nav glows all moved from purple/magenta to red; base surfaces moved from green-tinted to neutral blacks; Home headline now plain text with "Bhookmark" outlined + glowing, weather emoji removed (`contextualEmoji` deleted); `LIQUID_SPRING` retuned to near-critical damping (28/260/0.9) so motion settles without wobble; tab switches now have an exit animation (`AnimatePresence mode="wait"`); bottom-nav blur cut from 24px to 12px per the V03 budget; You-page settings, privacy note and sign-out folded into one collapsible "Account & settings" section; the four sibling docs now carry dated stale-banners listing what's wrong in them. **Not yet verified in a browser**: the You-page collapse and tab exit animation (local backend needs JWT secrets that live on Vercel, so no local sign-in). Saffron (trending) and coral (social) kept as distinct semantic hues. Scroll reveals (closed in round 3b): shared `Reveal` component (20px rise + fade at ~15% visibility, once, `REVEAL_SPRING`, static under reduced motion) on Home's lower sections and the Circles/Lists cards.
  - **Phase 3 round 2 (2026-09-13, after the user reviewed round 1 live on their phone)**: round 1's red was too bright and too present, and gold/saffron had crept in. Now: flat near-black (#0A0A0A) with dark blood red (#7A1219) mostly on hairline borders; `text-accent` lifted to #C9525A in index.css because dark blood fails text contrast; saffron/coral tokens remapped to blood tones, all per-category amber/teal/sky colours removed. Type moved from Unbounded + Hanken Grotesk to Inter Tight + Inter, with heavy weights capped at 600 and the lg–5xl scale shrunk ~2-3px; dish names use Instrument Serif in a hairline `.dish-name` frame. Home: engraved guilloche `Medallion` emblems replace category photos/emoji; "Restaurants near you" moved to the top; the trending stack moved to the bottom, shrunk to 21:10, and rotates daily through catalog dishes instead of always leading with the highest *seed* score (that was why Benne Masala Dosa was always first). **Ratings**: no rating numbers on any dish/venue card; the duel is gone (its score was anchored to seed-dish scores, which is why good dishes always came out ~8.9) and so is the hardcoded 6.8 fallback — the log flow now ends on a manual 0–10 `RatingPicker` the user must set, with a decisive verdict line from `ratingVerdict()` that never changes the number. Deleted: `Duel.tsx`, `StarRating.tsx`, `CravingOrb.tsx`, `EvidenceScoreBadge.tsx`, `dishesForSubtype`, `CATEGORY_SHADOW`, `CATEGORY_BORDER`. **Data issue, resolved 2026-09-13**: community scores in the `logs` table included duel-anchored values and test fixtures (e.g. the CTR "QA Travel Dosa"); the user chose to wipe all ratings and restart with real users (see round 3).
  - **Phase 3 round 3 (2026-09-13, commit ad47cdb, deployed)**: floating card interaction. `src/components/CardStage.tsx` provides `CardStageProvider` (app root), `StageShell` (recedes the tab content to 0.96), `FloatCard` (tap compress + light ripple, shared `layoutId`), `FloatMedia` (shared photo/medallion), and `DepthLayer` (staggered, pointer-parallax panel layers). The panel is portaled out of every clipping ancestor, uses separate wrappers for layout (FLOAT_SPRING, stiffness 140 / damping 18), 3D choreography (perspective 1200, translateZ 0→80→0, 4° tilt) and desktop parallax, adds a dimmed/blurred backdrop, a moving reflection plus a one-time SVG-displacement shimmer on decorative layers only, Esc/backdrop/close-button dismissal, focus trap + focus restore, body scroll lock, and reduced-motion fallbacks. Applied to Home recommended cards, the bottom stack's front card, category results, name-search results, Near Me venue cards (previously had no tap action at all — the reported "nothing happens" bug), and Bhookmarks log cards. Home's separate "profile" view is gone — `DishPanel` replaces it; Near Me has `VenuePanel` (reviews + Open in Maps by name/area, since venues carry no coordinates); Bhookmarks has `LogPanel`. Deleted `ScoreBadge.tsx`, `scoreEmoji`, and the unused emoji/swipeLabel verdict fields. Not applied to Craving Room swipe cards (tap is a game gesture there). **Round 3b**: Lists cards now lift into `ListPanel` (full numbered list, Clone and Counter-pick moved off the card so it's one tap target), and Food Circle cards lift into `CirclePanel` (fetches members + match ring itself), replacing the old circle-detail bottom sheet. **Verification caveat**: build + typecheck clean and opening renders correctly, but the automation Chrome tab reports `visibilityState: hidden` with zero rAF frames, so animation timing/smoothness, scroll-position return, and rapid-tap behaviour could not be judged there — needs a check on a real phone. **Ratings reset done (2026-09-13, at the user's explicit request)**: every row in `logs`, `log_deletions`, `idempotency_keys` and `venue_velocity` was deleted from production in one transaction (863 logs from 225 accounts); users (339) and venues (4,918) untouched. Full JSON backup of all four tables at `~/bhookmark-backups/2026-09-13-ratings-reset/`. Every account's Bhookmarks journal is now empty by design — ratings start fresh from real users.
  - **Visual design brief pass (2026-09-14, commit `5c554c6`, deployed to production after the user reviewed the local preview; `main` pushed)**: implements the user's "Bhookmark Visual Design and Animation Brief" (docx). It supersedes the round-2 "flat black + dark blood red" look.
    - **Tokens:** colours are CSS variables (RGB channels) behind the same Tailwind names, with two themes. Evening is the default (graphite `#151412`, espresso cards, ivory text, burgundy `#8E3340` actions, rose `#D89CA4` accent text; every `text-accent` became `text-rose`). Daylight is ivory/paper/ink. An Appearance sheet in You → Account & settings offers Evening, Daylight or Match device (`src/theme.ts`; an inline script in `index.html` applies it before first paint). Measured text contrast minimum: 4.78:1 Evening, 5.19:1 Daylight. Cards are opaque (the `.bg-surface` backdrop-blur override is gone), 22px radius, edge highlight plus soft shadow.
    - **Type:** `.dish-name` is Instrument Serif with no border and wraps (line-clamped on cards). The headline is solid text. Metadata moved to 13–16px and most tiny mono labels are gone.
    - **Imagery:** `CategoryArt`/`CategoryGlyph` (line drawings for all 14 real categories) replace the monogram `Medallion` (deleted). The guilloche rosette survives only as `FlavorPrint` on the Flavor DNA card. Cards no longer show another venue's photo as a stand-in (`categoryVisual().photo` fallbacks removed on Home/Bhookmarks). Recommended picks find their own catalog photo by name + venue.
    - **Crave:** search, then labelled craving chips, then the photo stack. Location personalisation and Near Me are one "Places near you" card, with a denied/unavailable state that points to cravings and name search. On mobile the stack comes before that card (`display: contents` + `order`). "While you were away" only shows real activity. Recommended cards show the name once plus venue/area (`src/format.ts` `placeLine` drops a venue name that repeats the dish name); the reason moved into the panel. Desktop (≥1024px) is a two-column discovery layout; other tabs cap at 620px.
    - **Panels:** Dish/Venue/Log panels put the log action right under the title and the recommendation reason last. On desktop they are photo + details side by side (`FloatCard wide`).
    - **Motion (`CardStage.tsx`):** `FLOAT_SPRING` is `visualDuration 0.38, bounce 0.12`. The lift peaks then keeps translateZ 16. The shimmer is 0.75s. Sheen and tilt only run with a fine pointer. The panel is bounded by safe areas. A 10px tap slop means a swipe never opens a card. A panel whose source card is gone fades in place. Focus returns as soon as the card is visible again.
    - **Framer exit hang (found in headless QA):** the shared-layout exit often never reports completion, near-every time when closing ~700ms after opening, rarely after it settles. The visual return still finishes in ~300ms; the leftover node is invisible. Mitigations: the exiting scene has `pointer-events: none` (`useIsPresent`), a 1.2s watchdog remounts `AnimatePresence`, and a tap during a close drops the old panel and opens the new one. Root cause not fixed.
    - **You:** a new user sees a "Log your first dish" invitation instead of three zero stats. Flavor DNA explains its unlock. A "Recent bites" strip grows from real logs. Settings are always visible rows. Removed: the hardcoded name "Navtej" (now the email local part or "Your passport"), the unearned "VERIFIED MEMBER" stamp, the placeholder "City leaderboard" card, and the contrarian card's false "city ranks it below 8.0" claim.
    - **Circles:** Craving Room lobby has distinct Host (filled) and Guest/join-with-code (outlined) cards.
    - **Dev-only fixes (`vite.config.ts`):** the `/dishes` proxy was sending `public/dishes/*.jpg` to the API, so photos 404'd locally (production was fine); a bypass now serves them. The proxy was missing `/friends`, `/circles`, `/rooms` and `/lists`.
    - **Verification:** `npm run build` and oxlint are clean; contrast was computed. A headless Chrome DevTools-protocol script (real animation frames; the Claude-in-Chrome tab is hidden and gets none) checked, at 402×874 and 1440×900 against the local API on the Neon test branch: open from a scrolled page (focus to Close, scroll lock, background 0.96, panel within viewport), Escape (focus back, scroll kept), close during open, triple activation (one panel), reopen during close, repeated cycles, horizontal drag (no open) vs still tap (opens) vs vertical swipe (scrolls), reduced motion (no scale or 3D transform), You/Circles/Bhookmarks and Daylight screenshots, and no horizontal overflow.
    - **Not done / open:** no "save" control (no save API exists — not invented). Onboarding still describes the removed duel swipes. The missing-source close path wasn't exercised. No real-phone check yet. Live check after deploy: bhookmark.com served bundle `index-S25UOvvo.js` / `index-DMKR7vUY.css` with the new UI strings, and no hardcoded name.
  - **Follow-up to the visual brief (2026-09-14, commits `07fbc8c` + `c59e183`, deployed; production `saved_dishes` migration applied)**: closes the brief pass's open items except the real-phone check.
    - **Production-only bug caught right after deploying:** `/saves` returned 404 on bhookmark.com because `vercel.json` lists API paths explicitly and the new route wasn't in it. The local dev proxy and the test suites couldn't catch this. Fixed and redeployed. Signed out, the live site now returns 401 for both reading and saving. **Any new API router needs a `vercel.json` rewrite as well as a `vite.config.ts` proxy entry.**
    - **Framer exit hang — root-caused and fixed (supersedes the watchdog workaround above):**
      - **Isolated with a headless-Chrome matrix.** Closing a panel ~300–1100ms after opening left it in the DOM every time; closing earlier or after it settled never did. Disabling layout crossfade, nested `layout`, the 3D exit keyframes, or adding `layoutDependency` made no difference. Skipping the ratings fetch removed the hang.
      - **Cause:** `DepthLayer`'s own `exit` on a layer that mounts after opening (the ratings block arriving from `/dishes/score`), closed while its entrance is still running, never resolves in Framer Motion 13.
      - **Fix:** `DepthLayer` no longer has an exit; the scene wrapper still animates the panel out. Result: 0 hangs in 56 attempts across 150–1300ms close timings, including after a forced remount.
      - **Cleanup:** the watchdog and all experiment flags are removed. Kept as cheap guards: the exiting scene ignores pointer events, and a tap during a close opens the new panel immediately.
    - **Save for later (new, zero-cost):**
      - **Backend:** `saved_dishes` table keyed by (user, normalised name + venue), because card sources' ids never line up; the display fields are a snapshot. `GET/POST/DELETE /saves`: private, idempotent, capped at 500, never feeds scores or recommendations (`server/src/routes/saves.ts`, `db.ts`). Migrated on the Neon **test branch only — production still needs `migrate.mjs` before deploying**. New `server/scripts/saves-test.mjs` (21 checks).
      - **Frontend:** `src/saves.tsx` provider with optimistic toggle, rollback and a status toast. `SaveButton`/`CardSaveButton` (a sibling of the card, never nested in its tap target) on the stack's front card, recommended cards, category results, and the dish and venue panels. A "Saved for later" strip in Bhookmarks opens the full dish panel. `SavesProvider` must wrap `CardStageProvider`, because panels render through the stage portal.
    - **Onboarding** rewritten for today's app (find, save, log with your own score); the removed duel/swipe copy is gone.
    - **Stray Vercel project:** confirmed gone (see Current problems 5).
    - **Verification:**
      - Build, frontend and server typecheck, and oxlint are clean.
      - Backend regression on the test branch: 157/157 (saves 21, nearby 16, qa-brief 24, recommendations 11, notifications 7, phase4 38, privacy-visibility 11, venue-claims 16, redteam-smoke 13).
      - Headless Chrome Save checks: card save is optimistic and confirmed without opening the card; persists across reload; the panel toggle works; the Bhookmarks strip shows it; unsaving from a saved card's own panel removes the source while open and the panel fades out in 334ms (the source-gone path, now exercised); offline goes saved → rolled back with a readable message and nothing stored; Onboarding shows the new steps.
      - Full motion regression re-run clean on the final code.
    - **Noticed, not changed:** Bhookmarks' header still uses the old uppercase mono label and shows a "preview: new account" debug toggle to users.
  - **Round 3: Flavor DNA fix, multi-dish visits, public photos, taste game (2026-09-14, uncommitted, NOT deployed)** — from the user's feedback after logging 3 real dishes. Decisions the user made: multi-dish = "Add another dish from this visit"; photos = Vercel Blob free Hobby tier; taste game AI = free Groq model with a no-AI fallback.
    - **Flavor DNA:** not a counting bug. The user's 3 logs were Matcha, Burger and Coffee, one each, and Flavor DNA counts per category, so "1 of 3" was accurate but the copy never said "in one category". Bhookmarks also used a different rule (loved-only) and claimed "eaten fresh off the counter — not delivered" with no data behind it. Now one shared `FlavorDnaCard` on You and Bhookmarks, one rule (all logs per category), copy naming the closest category ("Closest: Coffee, 1 of 3"). `evidenceThresholds.ts` exports thresholds and `categoryCounts`.
    - **Multi-dish visits:**
      - `logs.visit_id` column plus optional `visitId` on POST /logs.
      - The burst check counts visits, not dishes: a dish continuing a visit already logged at that venue adds no hit, so a real 6-dish order publishes. One visit is held past 12 dishes, and a visit id reused at another venue counts as a new visit.
      - BiteLog adds "+ Add another dish from this visit", which keeps the venue, location check and privacy; later dishes skip the location step, and the finish screen lists the visit's dishes. The stale "enter duels" offline copy is fixed.
      - Tests: `visits-test.mjs` 12/12.
    - **Public photos:**
      - Storage: Vercel Blob store `bhookmark-photos` (public, bom1), created and connected to production, preview and development; `BLOB_READ_WRITE_TOKEN` is on the Vercel project. `@vercel/blob` is in root and server package.json.
      - Upload: `POST /photos` (server upload, not browser-direct) takes a JPEG/PNG/WebP up to 1.5 MB, checks the magic bytes, stores it under `logs/{userId}/`, and records it in `photo_uploads`.
      - Caps: 30/user/day, and uploads pause at 1,800/month, under the Hobby plan's verified 2,000 uploads/month, 1 GB and 10 GB transfer; exceeding a limit stops Blob for 30 days.
      - Attaching: a log can only attach a photo its own account uploaded.
      - Showing: `/dishes/score` returns `photos` from public logs only; Near Me reviews carry `photoUrl`.
      - Moderation: `POST /photos/report` hides a photo after 2 different reporters; you can't report your own.
      - Frontend: compresses to 1280px JPEG (`src/compressImage.ts`, ~41 KB in the browser check); a failed upload never blocks the log. DishPanel gets a "Photos from real visits" strip with Report; the journal and Recent bites use the log's own photo.
      - Tests: `photos-test.mjs` 18/18 (it deletes its test blobs).
    - **Taste game:**
      - Unlock: 7 logs in a category from at least 2 places.
      - `/taste/status|round|answer|results`: 2–3-option "which place does {category} better?" questions about the person's own logged places, and "too close to call".
      - Storage: `taste_answers` holds pairwise rows; the ranking is Bradley–Terry with a half-win prior (`server/src/tasteRank.ts`); scores are never read or changed.
      - Suggestions: untried catalog places, cached in `taste_suggestions` until answers change or 24h pass. The AI step is optional: Groq `llama-3.1-8b-instant` (listed as production on Groq's model page), JSON mode, 4s timeout, picks validated against real candidate ids, and any failure falls back to ranking by community logs. The UI labels the source honestly.
      - **Groq free-tier limits are from third-party write-ups** (30 RPM, 14,400/day, 6,000 TPM, no card) and are not verified on Groq's own site. **No `GROQ_API_KEY` exists yet — the user has to create one; until then the game runs in fallback mode.**
      - UI: `TasteGameCard` on You and a full-screen `TasteGame`.
      - Tests: `taste-test.mjs` 23/23; `taste-ai-check.ts` 4/4 failure paths, with the live check skipped without a key.
    - **Routing:** `vercel.json` rewrites and dev proxy entries for `/photos` and `/taste`.
    - **Verification:**
      - Full backend regression green on the test branch: visits 12, photos 18, saves 21, nearby 16, qa-brief 24, recommendations 11, notifications 7, phase4 38, privacy-visibility 11, venue-claims 16, redteam 13, taste 23. Two runs hit "fetch failed" connecting to Neon (a network blip, including on untouched auth routes) and passed on rerun.
      - Headless browser: Flavor DNA copy, taste game card, a full round with 3-option and 2-option questions, results with ranking and 3 suggestions, clean close; BiteLog with a real photo, uploaded, compressed and served; dish 2 from the same visit (venue prefilled, location step skipped); a visit summary with both dishes; server confirms a shared visit id, the photo on dish 1 only, and the journal showing the uploaded photo.
      - Browser-posted logs show "held" because every request comes from 127.0.0.1 and trips the real multi-account-network hold; real users on distinct networks don't. Test scripts must send a per-user `X-Forwarded-For` on every request.
    - **Before deploy:** run `migrate.mjs` against production (visit_id, photo tables, taste tables) and get the user's go-ahead; add `GROQ_API_KEY` to Vercel when the user has one.
- Bhookmarks redesign (calendar, taste fingerprint, notes search), Bite Buddy / Taste Pulse (not built at all).
- ~~Phase 4: Craving Rooms / Food Circles / Lists~~ — **built and shipped** (2026-09-05). See the Phase 4 section above.
- ~~Phase 5 remainder: accessibility audit~~ — **done** (2026-09-04). Phase 5 is now fully closed.
- The formal manual QA click-through promised early in the project — Phase 4's flows (add friend, create circle, create/join a room, swipe, reveal, create/clone a list) got a real click-through pass this session; earlier phases are still only ad hoc. Today's Phase 1 stabilization was verified via curl + a real browser session, not a full manual device click-through — the brief's own scroll-gesture acceptance checks (iOS Safari + Android Chrome, reduced motion, slow/rapid scroll) still need a real phone.
- Data export + account deletion workflow (Phase 2 of the original brief, distinct from the "Phase 2" catalog rebuild above) — explicitly deferred by the user ("not needed for now, will see afterwards").

## Exact next step
Today's stabilization pass (build fix, scroll fixes, honesty fixes, P0 security fixes, Google Sign-In) is shipped and live. The catalog rebuild ("Phase 2" above) is in progress per the user's own approved plan, split across 5 sessions:
- **Session A (schema + alias tables) — done** (2026-09-11): `venues`, `dishes`, `dish_attributes`, `category_aliases`, `venue_submissions` tables live in prod.
- **Session B (OSM Overpass ingestion) — done, then extended** (2026-09-11): **4,907 real Bangalore venues are live in production** (`venues.source = 'osm'`). First pass covered only cafes/restaurants/fast-food (1,279 venues); the user caught that pubs and bars were entirely missing, and that dropping ~2,700 real venues just because their cuisine couldn't be guessed was the wrong tradeoff for an app whose actual requirement is "a user searches for a specific real place and it appears" — a venue existing shouldn't depend on being categorizable. Fixed: every real, named OSM venue is now inserted unconditionally into `venues`; a `dishes` category stub is created only when a category resolves. By category: 729 Coffee, 519 Bakery & Sweets, **314 Bars & Pubs** (new category), 165 Pizza, 151 Ice Cream, 123 Burger, 92 Chinese, 80 South Indian, plus 5 more. **2,669 venues have no resolved category** — real, present in the data, but not yet reachable by any live search (no name-search route exists yet — that's Session D). Verified on a real Neon test branch first (`server/.env.test`, branch "test"), then committed identically to prod both times. Three real bugs found and fixed while actually running this against live data (not caught by reasoning alone): `assertTestDatabase.mjs`'s test-branch detection never worked on Neon (branch hostnames are random, unrelated to branch name — fixed via `server/scripts/prodHost.mjs`); the venue uniqueness index collided on ~37% of real venues because most OSM entries lack a specific area tag (fixed by adding rounded lat/lng); and the venue upsert used `ON CONFLICT DO NOTHING`, which meant re-running after adding a new alias could never backfill a dish stub for an already-existing uncategorized venue (fixed to `DO UPDATE` so it always returns the id). Confirmed "Nandan Coffee" specifically isn't in OSM's data for this area — real coverage, not total coverage; the submission flow still matters for what OSM misses. Ingestion is slow (~4,900 candidates as sequential one-row-at-a-time DB calls, ~15-25 min per full run) — worth batching if this needs to run again at this scale or larger (e.g. expanding beyond Bangalore).
- **Session D (wire it into real search) — done** (2026-09-12): **the 4,907 venues are now actually reachable in the live app**, not just present in the database. `GET /venues/nearby` reads the real `venues`/`dishes` tables (category resolved through `category_aliases` first — structurally rules out the exact "Coffee"/"Filter Coffee" mismatch bug from ever recurring). New `GET /venues/search?q=` finds a real venue by name whether or not it has a resolved category — the actual fix for "when a user searches for a specific cafe/bakery/pub, it appears," verified end-to-end against the literal venue that started this whole thread: submitted "Nandan Coffee" through the new submission flow, approved it via the new `POST /moderation/venue-submissions/:id/approve`, and it was immediately findable by both search and nearby. New `POST /venues/submit` + moderation review routes are the fallback for whatever OSM doesn't have. `NearMe.tsx` now fetches its category list from `GET /venues/categories` instead of hardcoding a copy (rules out that class of drift structurally, not just for this one bug) and gained a "By name" search mode plus a "Don't see your spot? Add it" submission form. Two real bugs found while testing against live data: `/venues/nearby` fired one DB connection per nearby venue via `Promise.all` (100+ simultaneous connections for a popular category — exhausted even a real compute's connection limit, not just the tiny test branch; fixed with one batched query), and `computeLocationMatch`/`isImpossibleTravel` (GPS verification for real logs) only ever checked the old 13-venue hardcoded array — extended to fall back to the new venues table so GPS verification works for all 4,907 venues, not just the original 13. Verified: 16/16 rewritten `nearby-test.mjs`, 24/24 `qa-brief-tests.mjs` (one test's stale pre-Phase-1 assumption fixed along the way — see script comments), 13/13 `redteam-smoke.mjs`, 11/11 `privacy-visibility-test.mjs`, full build/lint clean, live curl verification against production. **Not yet done**: a real browser click-through — the Chrome extension wasn't connected this session, so the new "By name" search UI and Bars & Pubs category haven't been visually confirmed in the live app yet. Do that before considering Session D fully closed.
- **Session C (scoped) — done** (2026-09-12): a quick version, specifically to unblock Session E safely rather than the full "merge everything" version originally planned. Migrated the 12 original `catalog.ts` dishes' real diet/allergen tags into the new schema by re-seeding them as their own venues (`source='seed'`) at their known coordinates, rather than attempting to merge into an existing OSM row — a first attempt at coordinate-based matching found candidates up to 4.6km away for some names ("Empire Restaurant," "Toscano"), almost certainly a *different* real place with the same name elsewhere in the city. Force-matching would have risked attaching a real dietary claim to the wrong physical venue. Tradeoff, stated plainly: the same real place may show up twice for now (once via OSM with no diet data, once via this seed with it) until a real human-reviewed merge happens later. `server/scripts/migrate-catalog-diet-data.mjs`.
- **Session E (recommend.ts off the static catalog) — done** (2026-09-12): the last piece of the original plan. `computeNextPicks`/`computeDigest` now rank real dishes from the DB catalog (`db.listCatalogDishesWithScores()`) instead of the old 12-entry array, with a strict rule: any candidate with real log evidence outranks every candidate without one, full stop. Diet/allergen filtering treats unknown attributes as "not confirmed compatible," never "assume safe," now enforced against the real ~2,250-dish catalog instead of 12 hand-picked ones. Verified live: a fresh no-restriction account's top 3 picks were exactly the 3 real dishes with logged evidence; a dairy+gluten-allergic account (excluding all evidenced dishes) correctly fell through to real, unscored OSM venues with an honest "no community ratings yet" reason. `server/src/catalog.ts` deleted (confirmed dead). Found and fixed two things while verifying against full production data: a stale `recommendations-test.mjs` bug (fixed phone number + fixed idempotency keys meant repeat runs replayed cached responses instead of creating fresh logs — unrelated to this session's code changes, just never caught before), and a real query-performance investigation (7s cold / 2.6s warm-with-new-index for the recommendation query) that turned out to be Neon's serverless cold-start behavior — confirmed by observing the identical cold/warm pattern on the already-shipped `/venues/nearby` route against the same data, not something Session E introduced. Added `idx_logs_dish_lookup` regardless, a genuine improvement. **All 5 sessions of the original Phase 2 plan are now complete.**
- **Home's Crave tab wired to the real catalog — done** (2026-09-13): the one piece Session E's completion note flagged as still outstanding. `Home.tsx`'s category-browse and in-app search were still reading the old `src/data/dishes.ts` static array (~36 dishes, 7 categories) even after Near Me and `recommend.ts` had already moved to the real ~4,900-venue DB catalog — so a category tile in the Crave tab (e.g. "Coffee") showed the old fake set while the identical tile in Near Me showed the real one. Fixed: category tiles now come from `GET /venues/categories`; tapping one calls a new `GET /dishes/browse?category=` endpoint (`db.listCatalogDishesForBrowse`, evidence-first ranked, same honesty rules as everywhere else); in-app name search calls the existing `GET /venues/search`. Also fixed a real gap surfaced while wiring this up: `searchVenuesByName`/`SearchVenueRow` (`db.ts`) didn't return a dish's `subtype`, so a search-result click-through couldn't look up its exact evidence score — threaded `subtype` through `SearchVenueRow` → `/venues/search` → `VenueSearchResult` (frontend). Uncategorized venues (Session B's ~2,700) are shown honestly labeled "uncategorized," not silently dropped or given a guessed category. Verified: full server suite green (123/123 across all 7 scripts — nearby/qa-brief/recommendations/notifications/phase4/privacy-visibility/venue-claims), `tsc -b` and `npm run build`/lint clean on both frontend and backend, and a real browser session (test branch, then production) confirmed category browsing, name search — including multiple real "The Filter Coffee" branches, several uncategorized — profile view, and back-navigation all work with zero console errors. `src/data/dishes.ts`'s static array is now unused by both `Home.tsx` and `recommend.ts`; still imported for `dishById`/`categoryVisual`/`DISHES` fallback helpers (id-match fallback for the handful of legacy dish ids, category emoji/tint/photo lookup) — not yet fully dead code, but no longer a source of truth for what's browsable.

## Relevant files
- **Frontend core**: `src/screens/Home.tsx` (Crave/search, redesigned), `src/screens/LogFlow.tsx` (BiteLog, AI removed, privacy toggle added), `src/screens/Bhookmarks.tsx`, `src/screens/Profile.tsx` (venue-claim UI added), `src/screens/Duel.tsx`, `src/components/BrowseCard.tsx`, `src/smartPicks.ts`, `src/evidenceThresholds.ts`, `src/api.ts`
- **Phase 4 frontend**: `src/screens/Circles.tsx` (sub-tab shell), `src/screens/FoodCircles.tsx`, `src/screens/CravingRoom.tsx`, `src/screens/RemixableLists.tsx`
- **Backend core**: `server/src/app.ts` (trust proxy fix), `server/src/db.ts`, `server/src/routes/logs.ts` (ranking-manipulation signals, visibility), `server/src/routes/dishes.ts` (score separation, trending), `server/src/routes/venues.ts` (claims), `server/src/routes/moderation.ts` (claim review), `server/schema.sql`
- **Phase 4 backend**: `server/src/routes/friends.ts`, `server/src/routes/circles.ts`, `server/src/routes/rooms.ts`, `server/src/routes/lists.ts`
- **Test suites** (all in `server/scripts/`, run against a real Neon DB): `redteam-smoke.mjs`, `recommendations-test.mjs`, `nearby-test.mjs`, `notifications-test.mjs`, `qa-brief-tests.mjs`, `venue-claims-test.mjs`, `privacy-visibility-test.mjs`, `ip-isolation-test.ts`, `concurrency-100.mjs`, `phase4-test.mjs`; frontend unit test: `scripts/evidence-thresholds-test.mjs` (repo root)

## Test status (as of last full run, this session)
**89 backend tests + 30 Phase 4 tests + 4 IP-isolation tests, 0 failing**, plus 15 frontend unit tests — all green. Both frontend (`npx tsc -b`) and backend (`npx tsc --noEmit`) typecheck clean. Production deployed and verified healthy after every change this session, including a same-day follow-up deploy for the Craving Room swipe-freeze fix found in live QA.

**2026-09-10 re-run**: 38/38 `phase4-test.mjs` + 13/13 `redteam-smoke.mjs` still pass against the real database after today's Phase 1 stabilization changes. Not re-counted against the exact 2026-09-05 totals above, but no regressions found across either suite.

**Known test-infra fragility, now fixed**: three test files previously drew fake IPs from small (250-address) reserved ranges for `X-Forwarded-For` spoofing; repeated same-day runs eventually accumulated enough history that a random draw would collide with itself and trip the real multi-account-network detector, producing false test failures. All three now draw from `10.0.0.0/8` (~16.7M addresses), where this is no longer practically possible.

**2026-09-13 re-run** (Home Crave-tab wiring): all 7 test scripts pass against the real Neon test branch — 16/16 `nearby-test.mjs`, 24/24 `qa-brief-tests.mjs`, 11/11 `recommendations-test.mjs`, 7/7 `notifications-test.mjs`, 38/38 `phase4-test.mjs`, 11/11 `privacy-visibility-test.mjs`, 16/16 `venue-claims-test.mjs` — **123/123, 0 failing**. Frontend and backend `tsc -b` both clean, `npm run build` and `oxlint` clean (only pre-existing, unrelated warnings). Deployed to production and verified via `vercel inspect bhookmark.com` (correct deployment id/timestamp) plus live curl and a real browser session — no regressions.
